# Compiler
The LexcialAnalyzer

compile parse.cpp with

	g++ analyze.cpp -o analyzer

run with

    ./analyzer

The SyntaxParser

compile parse.cpp with

    g++ parse.cpp -o parser

run with

    ./parser
